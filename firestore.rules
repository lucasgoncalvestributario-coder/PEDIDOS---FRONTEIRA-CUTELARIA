rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Coleção de pedidos compartilhada entre Loja e Cuteleiro
    match /orders/{orderId} {
      allow read, write: if true;
    }
  }
}
